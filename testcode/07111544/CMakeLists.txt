project(07111544)

set(CMAKE_CXX_STANDARD 11)

set(CMAKE_CXX_STANDARD_REQUIRED ON)

include_directories(include)

set(SOURCE_FILES src/main.cpp)

set(HEADER_FILES)

add_executable(${PROJECT_NAME} ${SOURCE_FILES})
# Project Info: 
# IncludedClass:VideoConfig,Saving,CameraController,StorageManager,VideoRecorder,Frame,GPS,GPSModule,EncryptionModule,TimeManager,PolicemanInfo,
# Specification Path: 

