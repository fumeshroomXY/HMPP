project(07111550)

set(CMAKE_CXX_STANDARD 11)

set(CMAKE_CXX_STANDARD_REQUIRED ON)

include_directories(include)

set(SOURCE_FILES src/main.cpp)

set(HEADER_FILES)

add_executable(${PROJECT_NAME} ${SOURCE_FILES})
# Project Info: 
# IncludedClass:Frame,PolicemanInfo,VideoRecorder,GPSModule,EncryptionModule,Saving,StorageManager,VideoConfig,TimeManager,CameraController,GPS,
# Specification Path: 

